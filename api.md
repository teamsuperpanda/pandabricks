## Edge-to-Edge Migration Summary

This file recreates the earlier documentation that was not committed.

See `api.md` content previously described: edge-to-edge migration steps, dependency overrides, and validation process.

For expanded version, regenerate via assistant if needed.